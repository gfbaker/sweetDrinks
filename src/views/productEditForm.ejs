<!DOCTYPE html>
<html lang="en">

    <!-- HEAD -->
    <%- include('./partials/head',{title: 'Editando: ' + productToEdit.nombre}) %>
    <!-- FIN-HEAD -->

<body class="body">

        <!-- HEADER -->
        <%- include('./partials/header') %>
        <!-- FIN DEL HEADER -->

    <!-- MAIN -->
    <main class="mainProducto-editar">
        <div class="wrapper-formulario">
            <div class="formulario-titulo">
                <div class="formulario-titulo">
                    <h2 class="formulario-titulo">Estás editando: <i><%= productToEdit.nombre %> <%= productToEdit.volumen %></i></h2>
                </div>
            </div>
            <div class="formulario-cuerpo">
                <form action="/products/<%= productToEdit.id %>?_method=PUT" method="POST" class="form-ProductoNuevo">
                    <div class="estilo-formulario">
                        <label for="nombre" class="label-formulario">Nombre del Producto:</label>
                        <input type="text" id="nombre" value="<%= productToEdit.nombre %>" name="nombre" class="estilo-input">
                    </div>
                    <div class="estilo-formulario">
                        <label for="volumen" class="label-formulario">Volumen:</label>
                        <input type="text"  id="volumen" value="<%= productToEdit.volumen %>" name="volumen" class="estilo-input">
                    </div>
                    <div class="estilo-formulario">
                        <label for="precio" class="label-formulario">Precio:</label>
                        <input type="text"  id="precio" value="<%= productToEdit.precio %>" name="precio" class="estilo-input">
                    </div>
                    <div class="estilo-formulario">
                        <label for="porcentajeAlcohol" class="label-formulario">Porcentaje de Alcohol:</label>
                        <input type="number" id="porcentajeAlcohol" value="<%= productToEdit.porcentajeAlcohol %>" name="porcentajeAlcohol" class="estilo-input">
                    </div>
                    <div class="estilo-formulario">
                        <label for="stock" class="label-formulario">Cantidad disponible: </label>
                        <input type="number" id="stock" value="<%= productToEdit.stock %>" name="stock" class="estilo-input">
                    </div> 
                    <div class="estilo-formulario">
                        <label for="descripcion" class="label-formulario">Descripción: </label>
                        <textarea name="descripcion" id="descripcion" name="descripcion" class="estilo-input"><%= productToEdit.descripcion %></textarea>
                    </div>
                    <div class="form-div-checkbox">
                        <input type="checkbox"  id="oferta" name="oferta" class="form-checkbox" <% if (productToEdit.oferta == true){%>
                            checked
                            <% } %>
                        >
                        <label for="oferta" class="form-checkbox">En oferta</label>
                    </div>
                    <div class="estilo-formulario">
                        <label for="descuento" class="label-formulario">Descuento: </label>
                        <input type="number" id="descuento" value="<%= productToEdit.descuento %>" name="descuento" class="estilo-input">
                    </div> 
                    <div class="estilo-formulario">
                        <input type="checkbox"  id="importado" name="importado" class="" <% if (productToEdit.importado == true){%>
                            checked
                            <% } %>
                        >
                        <label for="importado" class="form-checkbox">Importado</label>
                    </div>
                    <div class="estilo-formulario">
                        <input type="checkbox"  id="pack" name="esPack" class="" <% if (productToEdit.esPack == true){%>
                            checked
                            <% } %>
                        >
                        <label for="pack" class="estilo-input">Es un pack?</label>
                    </div>
                    <div class="estilo-formulario">
                        <label for="imagenes" class="label-formulario">Imágenes: </label>
                        <input type="file" id="imagenes"  name="imagenes" class="estilo-input" multiple>
                    </div>
                    <div class="estilo-formulario">
                        <button type="submit" class="boton-formularios">Guardar</button>
                    </div>
                </form>
            </div>
            
        </div>

    </main>
    <!-- FIN-MAIN -->


    <!----- FOOTER ----->
    <%- include('./partials/footer') %>
    <!----- FIN-FOOTER ----->

</body>

</html>